{
  "name": "black-messenger",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://Blackbot420.github.io/Black-Messenger",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
